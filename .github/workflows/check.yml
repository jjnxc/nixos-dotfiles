name: flake-check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
      # Not --no-build: catppuccin imports files from derivations at eval time (IFD).
      - name: Check flake evaluates
        run: nix flake check -L
      - name: Check formatting
        run: nix fmt -- --ci
      - name: Lint with statix and deadnix
        run: |
          nix develop --command statix check .
          nix develop --command deadnix --fail --exclude hosts/desktop-nvidia/hardware-configuration.nix .
